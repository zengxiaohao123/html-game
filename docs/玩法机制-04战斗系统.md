# 异世界流浪日记 · 玩法机制 · 战斗系统

> 本文是《异世界流浪日记》玩法机制文档系列的第四篇，对应游戏代码模块 **「战斗系统」（js/combat.js）**。
> 只描述当前代码已实现的战斗逻辑；🧩 为**规划中/未实现**，见文末附录。

---

## 六、战斗系统（js/combat.js）

### 6.0 一次战斗的完整流程（伪代码）

```
startCombat(进入战斗格) → initCombatState（生成敌人、记录 startSnapshot、planEnemyTurn 锁定敌人意图）→ enterCombatMode

每回合：
  1) 玩家操作（当前角色 currentChar）：
     - 用 1/2/3 选技能（再按一次 或 按 Q）→ 移动前主动释放；或直接 WASD / 点格+「前往」移动。
     - 移动即结束我方回合（playerMoved=true）。
     - 移动后：autoCastAll 让全队依次自动释放各自已选技能（无目标则跳过）。
  2) endPlayerPhase → 敌人回合 enemyTurn（按锁定计划执行移动/朝向/技能）→ turn++
  3) 结算 poison 中毒流失 → 我方/敌方状态 tick（倒计时）→ 解析 水泡 → 护盾刷新（=防御力）→ 玩家可再次行动 → planEnemyTurn 锁定敌人下一回合意图
重复直到 敌人全灭(胜利) 或 主角 hp<=0(失败)。
```

### 6.1 地图与进入/结束

- 与探索共用同一张格子地图；进入战斗后切交互模式（`mode=combat`），探索态其它内容不参与战斗。
- **战斗结束强制传送回进入战斗那一格**（`entryCell`），该格战斗内容清空变空地。
- 战斗移动**不消耗探索 AP**；每次真实移动仍消耗已选载具（见 05）。

### 6.2 我方操作

- **角色切换**：鼠标点角色卡 或 `F1/F2/F3`（当前角色 `currentChar`）。
- **技能选择**：`1/2/3` 选技能、`4` 选中「逃走」；再次按下所选技能（或按 `Q`、或双击技能）主动释放；技能种类分 攻击/辅助。
- **释放时机**：移动前主动释放 或 移动后系统自动全队释放。每个角色每回合至多 1 次技能。
- **移动**：WASD 或 点击相邻格+「前往」，二选一即结束我方回合；朝障碍/地图外只转向并计入已移动。
- **保护动限制**：被【禁锢】时移动无实际效果（仅用于结束回合）。

### 6.3 敌人 AI（`resolveEnemyIntent` / `planEnemyTurn`）

- 每回合开始锁定敌人意图（`plan`）：朝向、移动第一步、技能**按当前局势固化**，玩家操作不改变。
- 从敌人技能中选择：优先选用攻击技（排除 `slimebang` 的专用判断 + `onlyTurn1` 首回合限制 + 冷却就绪），其次辅助技，再回退通用近战；机关为随机选攻击技（前 5 次不用大清扫）。选技能后判断能否命中主角（`hitAt`/`hitByFace`/`hitByMove`）——打得到就攻击、够不着就 `firstMoveStep`/`pathStep` 逼近（BFS 最短路径绕障，猎犬用 2 格追逐）。
- **移动**：敌人一回合可同时“移动 + 技能”；无法到达攻击距离时只移动。机关满血走「巡逻」随机方向。
- **攻击目标**：只攻击主角（队友视为同单位，地图只显示主角）。
- **多敌人**：敌方阶段所有存活敌人依次行动（狗友/集群生效）。

### 6.4 伤害与抗性（当前简化公式）

```
双方基础攻击：
  主角: charAtk('pro') = G.hero.atk + 各含 atk 的天赋加成 + 攻击提升(atkUp +25%)
  队友: c.atk + 各含 atk 的天赋加成
  敌人: 生成时 atk × 编队加成

我方 → 敌人:
  dmg = round( 攻击力 × (1 − 敌对应抗性/100) × 技能倍率 × (1 + 起势加成 mom/100) )，最小1
  暴击率 =（天赋暴击 + 屏息/比翼 + ）限幅 0~100%；命中暴击 → dmg × 2
  元素亲和免疫（史莱姆）：免疫对应元素伤害，且不掉血、不上元素
  受击天赋：帅气闪避(20%归零)、古树反击(来源40%真伤 + 自身atk+15%叠至+150%)
  命中后处理：嗜血回复、起势叠加、燃烧、【重点目标】、元素附着、战败检查

敌人 → 主角:
  敌人技能倍率 → 伤害
  格挡（主角天赋）概率 → 伤害归 0
  护盾（=防御力，每回合刷新）先吸收等量伤害（真实伤害无视护盾）
  坚守 概率 → 回复 12% 最大生命
  hp<=0 → 战败
```

- **防御语义**：主角 `def` 作为**护盾**，每回合初刷新=防御力对应值（`refreshHeroShield`）；不参与常规减伤公式。
- **暴击**：初始暴击率 0，暴击伤害固定 +100%（×2）；暴击率最终限 0~100%；「下次攻击暴击率+100%」用一次攻击型技能后提前失效。

### 6.5 状态系统

- 状态栏：buff 黄框 / debuff 红框 / 中性灰；主角额外展示全场状态。
- 状态**点击**查看详情；词条【】**悬浮**查看解释。
- 状态倒计时 `turns` 在每回合结束 tick；带 `layers`（中毒层数）累加叠加。
- 已实装状态见 `02·十三`（燃烧/束缚/攻击提升/护盾/重点目标/伤害减免/屏息/禁锢/中毒/睡眠）。
- 附带判定：敌人受攻击且处于【睡眠】→ 生命降低提前醒来；主角被【禁锢】/【束缚】时移动无实际效果。【燃烧】每回合初流失2%最大生命、【中毒】流失对应层数。

### 6.6 逃跑（`tryFlee`）

- 前提：本回合**未移动、未使用主角技能**（队友仍照常自动释放）。
- 成功率 `calcEscapeRate`：`(= (主角逃跑速度 − 敌人速度×剩余生命%) / 100)`，限 0~100%，并在 UI 显示。
- 成功：战斗立即结束、无奖励、**逃跑速度永久 +1**，该格变空地。
- 失败：主角本回合失去行动（跳过），队友照常释放。

### 6.7 战斗胜负与奖励

- **胜利**：击败本场全部敌人 → `endCombat(true)`。
- **失败**：主角 hp<=0 → `endCombatByDefeat`，hp 置 1，健康 -healthPenalty，提示“战斗失败”。健康归零 → Game Over。
- **奖励结算**（`grantVictoryRewards`）：按敌人 `reward` 发物品 + `attrUp` 次属性升级（二选一=攻击+1 / 最大生命+5；三选一加 防御+1；`doubleUp` 双倍、额外掉落 `rate`/`coin5`；狗友生成的猎犬不计奖励）。
- 编队人数加成：1/2/3 人 → 敌 atk ×1.00/1.15/1.30、maxHp ×1.00/1.20/1.40（`teamSizeBonus`）。

### 6.8 元素亲和与附着（✅ 当前已实现）

- 史莱姆系列：`AFFIN_IMMUNE` 对应元素**免疫**且常附着该元素（`aura`）。
- `setAura` 设置元素附着标记；附着会顶替旧附着。
- 🧩 **完整元素反应表（蒸发/燃烧/超载/融化/冻结/绽放/感电/激化/超导/扩散/结晶）尚未接入**，仅有“亲和免疫 + 附着标记”这一层，反应触发计算见文末附录B（规划中）。

---

## 七、规划中 / 未实现清单（战斗相关）

- ❌ 完整元素反应结算（见附录B，仅保留规格供后续实现）。
- ❌ 召唤物（草史莱姆/水精灵）、结界（绽放/离离枯荣境界）、【额外回合】、多段反应。
- ❌ 队友完整个人天赋结算（更多见 `02·十二`）。

---

## 附录B 元素反应表（🧩 规划中 · 保留规格）

> 下列反应在 `combat.js` **未实现**，仅作后续开发参考；当前游戏只做了元素亲和免疫与附着标记。

| 触发(附着+受击) | 反应 | 效果 |
|---|---|---|
| 火 + 水伤 | 蒸发 | 本次水伤最终伤害 +50% |
| 火 + 草伤 | 燃烧 | 目标【燃烧】3回合：每回合初流失2%生命 |
| 火 + 雷伤 | 超载 | 目标及周围4格同阵营 `施加者atk×20%` 火伤（不再二次附着） |
| 火 + 冰伤 | 融化 | 本次冰伤最终伤害 +100% |
| 水 + 冰伤 | 冻结 | 目标【冰冻】1回合：无法行动 |
| 水 + 草伤 | 绽放 | 周围9格随机1空格生成我方【草史莱姆】 |
| 水 + 雷伤 | 感电 | 对目标及周围9格随机1同阵营单位递归 `atk×10%` 雷伤（至多5名） |
| 草 + 雷伤 | 激化 | 目标【激化】2回合：ATK+15%、受雷/草伤害+25% |
| 雷 + 冰伤 | 超导 | 目标【超导】3回合：雷/冰/物理抗性−30%（限0~90%） |
| {火/水/雷/冰}附着+ 风伤 | 扩散 | 目标及2格内同阵营 `atk×30%` 对应元素伤害 |
| {火/水/雷/冰}附着+ 岩伤 | 结晶 | 施加者获得 `自身def×150%` 护盾 |

---

> 本分篇为玩法机制文档系列的一部分。代码实现见 `js/combat.js`；词条完整库见 `02·十三/十四`。