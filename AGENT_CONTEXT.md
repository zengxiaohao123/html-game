# AGENT_CONTEXT.md

> 给后续 AI Agent 看的锚点文件。新对话直接读本文件即可完整继承上下文。
> ⚠️ 本文件的每一条内容**只能来自本次对话（用户亲口说过的 + Agent 亲身验证过的）**，禁止引用其他会话的记忆或用户画像标签。

---

## 一、项目元信息（已确认）

| Field | Value |
|---|---|
| GitHub 仓库 | `zengxiaohao123/html-game` |
| GitHub 主页 | `https://github.com/zengxiaohao123` |
| Cloudflare 部署网址 | `https://html-game-ap9.pages.dev/` |
| 默认分支 | `main` |
| 构建方式 | 纯静态 HTML/JS，无构建步骤，Build command 留空，输出目录填 `/` |
| Cloudflare 绑定 | ✅ 已绑定本仓库，main 分支有新 commit 时自动部署（约 10 秒生效） |
| 部署网址国内直连 | ✅ 移动/联通/电信均无需翻墙 |

## 二、已明确的约束条件（用户说过的原话）

- 单机游戏，**没有后端服务器**。存档用浏览器 localStorage 或 IndexedDB，纯本地保存。
- 不考虑排行榜，不读取其他玩家数据，不向任何外部服务发送用户游戏数据。
- 代码全部存 GitHub 仓库，Cloudflare Pages 自动托管。
- 不引用需要翻墙的外部 CDN（国内能访问的 jsDelivr / unpkg 可以）。
- **前端一切展示文本必须中文**（中文游戏）；代码/变量可英文，前端渲染必中文。
- 逃跑系统尚未做，目前只显示逃跑速度，暂不做真正逃跑；敌人的普通攻击也算一种技能，写入技能信息。

## 三、连接状态（Agent 2026-09-01 实际测过）

- GitHub MCP **写入权限已实测**：create / update / delete / get 全部成功，且已验证临时文件写入后删除，仓库干净复原。
- GitHub → Cloudflare Pages webhook **已绑定**：改 GitHub main 分支 → CF 自动构建部署。

## 四、当前进度

- [x] GitHub 仓库创建，Cloudflare Pages 绑定
- [x] MCP 读写链路实测通过
- [x] 本锚点文件写入仓库
- [x] 玩法机制文档**已拆分为多篇（docs/ 目录，按模块分类）**：
  - 源《玩法机制介绍-总篇》已按模块分为 6 篇：`docs/玩法机制-01总览与约束 / 02数据与常量 / 03地图与探索 / 04战斗系统 / 05界面与UI / 06存档与进度`。它们共同构成玩法机制的**唯一权威来源（SSOT）**；第一篇含「文档系列构成」表。原单篇两个同名文件已删除。
- [x] **游戏本体已实现并部署（可试玩）**
  - 已实现：每天随机地图（尺寸随天数渐进）、WASD/点格+前往连续移动+朝向、资源递进采集(0/2/4/6)、宝箱/事件格、回合战斗(WASD/Q/点敌选目标)、主角5攻击技、敌人逼近/贴身攻击、基础元素反应(蒸发/燃烧/绽放)、睡觉进下一天、8槽localStorage存档、健康归零GameOver、主菜单/背包/角色/编队/任务/商店
  - 已修复：①战斗击杀后 `combatState` 置空越界访问(加空守卫)；②启动主菜单无按钮/页面只显示标题的**根因**是 `$` 辅助函数误用 `document.getElementById(id)`、而全代码统一按 `$('#id')`（带 `#` 前缀）调用，导致启动 `switchMode('story')` 时 `$('#bottom')` 返回 null 抛 `TypeError`，`load` 处理器中断、`showMenu()` 永不执行。已改为 `const $=id=>document.querySelector(id)`（2026-09-03 曾在线上复现该报错并据此修复）；③只能移动1次(goBtn onclick 里 `disabled=true` 永久禁用，改为 display 隐藏+每次预览重启用)；④探索 WASD 连续移动
  - **UI 重构(2026-09-02)**：图标行顺序=任务/编队/角色/背包/睡觉/设置/商店(睡觉独立按钮不进设置)；左=行動记录(不叫文字区)；右标题 探索"事件 / 选择"、战斗"插曲"；底部"剧情/战斗技能"同区按 mode-story/mode-combat 切换、不并存；大面板(背包/角色/编队/任务近全屏可滚动)；前端全中文(資源映射 RES_ZH、生命≠HP、第X天≠Dx)
  - **代码模块化重构(2026-09-03)**：把单文件 `index.html` 拆为 `index.html`(仅结构+引入) + `css/style.css` + `js/{data,save,map,explore,combat,ui,main}.js`。用**经典全局 script 按依赖顺序加载**（不用 ES Module），函数/变量保持全局，内联 `onclick="startNew()"` 等仍可命中全局函数。加载顺序：data → save → map → explore → combat → ui → main。
  - **大改版(2026-09-03)**：队友占位(夏阳·火/陆悠悠·风)；移动逻辑(探索转向返还AP、战斗朝障碍计已移动)；攻击朝向；UI区域分工(左行动记录/中地图/右信息区/底部剧情或角色技能区)；地图不规则+随机出生+缩放拖拽；敌人红色朝向三角+范围指示器(橙/红)；任务/编队/角色/背包/设置全屏面板；行动记录/剧情区在战斗与睡觉时清空。
  - **状态系统+UI(2026-09-03，本轮)**：
    - 真实状态系统：角色/敌人各带 buff/debuff+持续回合（`combatState.ally[k].statuses` / `enemy.statuses` / `combatState.field`；`addStatus/tickStatuses`）；技能效果接入（燃烧/伤害减免/攻击提升/屏息/重点目标）。
    - 状态栏：角色技能区每角色独立状态栏（正面黄框/负面红框/中性灰框），过多时**横向滚动**；主角额外显示全场结界/境界（field）。
    - 敌人：新增技能数据（普攻也算技能）；信息区点击敌人=**两页**（属性[数值/状态栏/意图/朝向] / 详细技能），可切换。
    - 数值化技能/天赋描述：技能描述去文学化，伤害以（当前约X点）显示（纳入我方强化，不计算敌人减伤）；描述占位 {X} 伤害 / {Y} 治疗。
    - 词条：技能描述中【词条】下划线+悬浮弹窗（`terms()` 转 termHTML）；天赋点击在**天赋附近**弹窗（#popover），点别处关闭。
    - 编队：可调换123号位（影响自动释放顺序）；角色页每角色一页（点名字切换）+携带技能选择（至多3，天赋不算）。
    - 数值：敌人HP上调（哥布林420/毒蛾330/岩狼520）确保多回合观察行动记录。
    - UI微调：行动记录区缩至约1/3屏宽；选中技能=浅金背景（非边框）；辅助技能蓝框；角色技能区属性只留 攻击/生命/防御/逃跑速度；角色名上边框遮挡微调。
    - **已搁置/待做（工作记录）**：天赋实际效果多为占位（涅槃复活、心想事成·巧遇、好奇心、暴击/嗜血/起势/格挡/坚守、风息/比翼等），只做了文本描述未接逻辑；敌人技能暂无真实伤害效果（仅介绍）；结界/境界/全场效果仅搭了展示位（field）未接任何技能；束缚/冰冻等状态已建模型但暂无技能施加。
  - **未实现(见文档 01·总览)**：完整剧情线、图鉴、商店定价、更多敌人、完整伤害乘区、心理压力、剩余队友（许泠朦/叶唯安/潘天宇）数据

## 五、游戏设计要点（基于权威规格书，供 Agent 开发时参考）

- **类型**：回合制战斗 + 文字剧情 + 少量推理、数值养成/阵容搭配、少量生存经营的单机游戏；非肉鸽。
- **两个核心模式（共用一张格子地图）**：探索（收集资源）与战斗（回合制）；战斗结束强制传送回进入格，战斗内破坏战后还原。
- **天数系统**：每天随机重生地图（尺寸随天数渐进变大）；睡觉=进下一天+自动存档；health=全局健康值，归零即游戏结束。
- **战斗**：我方移动即视为回合结束；移动前可主动放技能(Q/双击技能)、移动后自动释放已选技能（队伍一二三号位顺序，已用跳过）；WASD/点格+前往移动；朝向决定部分攻击；方向性技能需方向上有敌人才生效。
- **状态系统**：角色/敌人各自有状态栏；buff正面黄框、debuff负面红框、中性灰；主角额外记录全场结界/境界；状态过多横向滚动。
- **伤害公式/元素反应**：见 docs/04·战斗系统（pseudocode + 附录B 反应表）。
- **队伍**：含主角最多3人；攻击+辅助技能每角色共最多带3个；每回合每角色至多1技；编队可调换123号位、角色页可自选携带技能。
- **未实现/待定**：潘天宇数据、敌人数据、商店数值、数值平衡、剧情分支、图鉴内容、行动力/心理压力系统等 —— 均在 docs/01·总览 的「十五、待定清单」，**未确认前不要脑补**。
- **MVP 完成情况**：核心循环可用；下一步按规格书补齐队友角色与技能、剧情与图鉴、商店与更多敌人、完整乘区与更多反应。

## 六、美术 / 文字定位（规格书第十章）

- 极简：文字+数值为主体，极少立绘（有也用图标），区域用背景/区域色。
- 元素着色：火red/水blue/草green/雷purple/冰cyan/风lightBlue/岩brown；技能名里的元素词不改色（如「似水流年」的水）。
- 词条：技能描述【】内效果名=词条，下划线+悬浮弹窗解释（完整库见 docs/02·数据与常量 附录A）。
- **前端口径一律中文**（见 docs/05·界面与UI 的 10.4）。

## 七、Agent 操作规范（用户明确要求，必须遵守）

1. **所有锚点文件内容，只能来自本次对话**。禁止从其他会话的记忆、用户画像标签、经验回顾里搬运任何未经用户再次确认的信息。
2. **每次修改代码或修改网页后，必须同步更新本锚点文件**。更新时不要把新内容堆在末尾 —— 要整理分类，删除已过时的旧条目，在合适位置插入新的修改记录。
3. **修改代码时，直接删除旧内容，不要保留删除线+新内容这种双写结构**。

## 八、给后续 Agent 的快速上手流程

```javascript
// 1) 先读本文件
// 2) 再读仓库当前状态
get_file_contents({ owner: "zengxiaohao123", repo: "html-game", path: "" })
//  玩法机制权威来源：docs/玩法机制-01~06
//  代码结构：index.html 引入 css/style.css + js/{data,save,map,explore,combat,ui,main}.js
// 3) 改代码：用 create_or_update_file / push_files 推 main 分支（push_files 必须内联 files[].content）
// 4) Cloudflare 自动部署后，用 WebFetch 验证
// 5) 改完代码不要忘了：回到本文件按第七条规则同步更新
```

## 九、部署故障排查

如果 Cloudflare 网址打不开但 GitHub 有文件：
1. Cloudflare Pages 控制台 → html-game → Deployments，看最新构建是否成功
2. 如失败，查看构建日志错误（常见：路径写错、main 分支未选中、index.html 不在根目录）
3. 修复后重新 commit 触发自动重部署
4. **页面只显示标题、无主菜单按钮，且已确认线上 JS 就是新的（含 `const $=id=>document.querySelector(id)`）→ 多为浏览器/边缘缓存命中旧 JS，提示用户 硬刷新(Ctrl+F5) 或 无痕窗口，或带唯一 query 参数访问。若线上 JS 仍是旧的 `getElementById` 版本 → 是旧部署，重新 commit 触发 CF 重部署。**