# AGENT_CONTEXT.md

> 给后续 AI Agent 看的锚点文件。新对话直接读本文件即可完整继承上下文。
> ⚠️ 本文件的每一条内容**只能来自本次对话（用户亲口说过的 + Agent 亲身验证过的）**，禁止引用其他会话的记忆或用户画像标签。

---

## 一、项目元信息（已确认）

| Field | Value |
|---|---|
| GitHub 仓库 | `zengxiaohao123/html-game` |
| GitHub 主页 | `https://github.com/zengxiaohao123` |
| Cloudflare 部署网址 | `https://html-game-ap9.pages.dev/` |
| 默认分支 | `main` |
| 构建方式 | 纯静态 HTML/JS，无构建步骤，Build command 留空，输出目录填 `/` |
| Cloudflare 绑定 | ✅ 已绑定本仓库，main 分支有新 commit 时自动部署（约 10 秒生效） |
| 部署网址国内直连 | ✅ 移动/联通/电信均无需翻墙 |

## 二、已明确的约束条件（用户说过的原话）

- 单机游戏，**没有后端服务器**。存档用浏览器 localStorage 或 IndexedDB，纯本地保存。
- 不考虑排行榜，不读取其他玩家数据，不向任何外部服务发送用户游戏数据。
- 代码全部存 GitHub 仓库，Cloudflare Pages 自动托管。
- 不引用需要翻墙的外部 CDN（国内能访问的 jsDelivr / unpkg 可以）。
- **前端一切展示文本必须中文**（中文游戏）；代码/变量可英文，前端渲染必中文。
- 逃跑系统尚未做，目前只显示逃跑速度，暂不做真正逃跑；敌人的普通攻击也算一种技能，写入技能信息。
- 文字配色：**一律浅色**（白/灰白/红/黄/金），慎用灰黑（易看成黑）；仅在白色背景上才可用黑字。

## 三、连接状态（Agent 2026-09-01 实际测过）

- GitHub MCP **写入权限已实测**：create / update / delete / get 全部成功，且已验证临时文件写入后删除，仓库干净复原。
- GitHub → Cloudflare Pages webhook **已绑定**：改 GitHub main 分支 → CF 自动构建部署。

## 四、当前进度

- [x] GitHub 仓库创建，Cloudflare Pages 绑定
- [x] MCP 读写链路实测通过
- [x] 本锚点文件写入仓库
- [x] 玩法机制文档**已拆分为多篇（docs/ 目录，按模块分类）**：
  - 源《玩法机制介绍-总篇》已按模块分为 6 篇：`docs/玩法机制-01总览与约束 / 02数据与常量 / 03地图与探索 / 04战斗系统 / 05界面与UI / 06存档与进度`。它们共同构成玩法机制的**唯一权威来源（SSOT）**；第一篇含「文档系列构成」表。
- [x] **游戏本体已实现并部署（可试玩）**
  - 已实现：每天随机地图（尺寸随天数渐进）、WASD/点格+前往连续移动+朝向、资源递进采集(0/2/4/6)、宝箱/事件格、回合战斗(WASD/Q/点敌选目标)、主角5攻击技、敌人逼近/贴身攻击、基础元素反应(蒸发/燃烧/绽放)、睡觉进下一天、8槽localStorage存档、健康归零GameOver、主菜单/背包/角色/编队/任务/商店
  - 已修复：①战斗击杀后 `combatState` 置空越界访问(加空守卫)；②启动主菜单无按钮的根因是 `$` 辅助函数误用 `getElementById`，已改 `const $=id=>document.querySelector(id)`（2026-09-03 线上复现并修复）；③只能移动1次(goBtn 永久禁用，改 display 隐藏+预览重启用)；④探索 WASD 连续移动
  - **UI 重构(2026-09-02)**：图标行顺序=任务/编队/角色/背包/睡觉/设置/商店；左=行動记录；右=信息；底部"剧情/战斗技能"同区按 mode-story/mode-combat 切换；大面板近全屏；前端全中文。
  - **代码模块化重构(2026-09-03)**：`index.html`(仅结构+引入) + `css/style.css` + `js/{data,save,map,explore,combat,ui,main}.js`。经典全局 script 按依赖顺序加载（data → save → map → explore → combat → ui → main）。
  - **大改版(2026-09-03)**：队友占位(夏阳·火/陆悠悠·风)；移动逻辑(探索转向返还AP、战斗朝障碍计已移动)；攻击朝向；UI区域分工；地图不规则+随机出生+缩放拖拽；敌人红色朝向三角+范围指示器；全屏面板；行动记录/剧情区清空。
  - **状态系统+UI(2026-09-03)**：真实状态系统（`ally[k].statuses`/`enemy.statuses`/`field`，`addStatus/tickStatuses`）；状态栏（正面黄框/负面红框/中性灰框，横向滚动，主角额外显示全场）；敌人技能数据（普攻也算技能）+信息区两页(属性/详细技能)；技能/天赋数值化描述（{X}伤害/{Y}治疗）；编队调换123号位+角色页携带技能选择；敌人HP上调(哥布林420/毒蛾330/岩狼520)；行动记录约1/3屏宽；选中技能浅金背景、辅助蓝框。
  - **本轮(2026-09-03)**：
    - 明确区分：**状态栏状态=鼠标点击查看详情**（#popover 弹窗，含名称/持续回合/描述）；**词条【】=鼠标悬浮查看**（#tooltip，词条带下划线）。已把 termHTML 的 title 去掉、状态 chip 改为点击。
    - 删除剧情区"剧情"与角色技能区"战斗·角色技能"两个标题，空间还给内容，总高度不变。
    - 地形：可通行(空地)加大到约80%，减少不可通行/地图外；命名 空地=可通行、山脉/地图外=不可通行。
    - 技能指示器：敌我技能范围**不含不可通行格**；敌方指示器默认不显示，仅点击该敌人所在格才显示该敌人的攻击范围。
    - 信息区：战斗开始/结束/睡觉时刷新（结束/睡觉清空）；敌人意图详细（是否移动/如何移动/是否攻击/用哪个技能）；**不再显示朝向**（由地图三角负责）；**元素附着并入状态栏**（不再单独列出/不再单独地图小标签）；已探索的宝箱/事件格再次点击显示为"空地"。
    - 技能区：角色属性写进角色卡名字附近（miniAttrs）；原数值行缩小为只突出攻击力；状态栏加大。
    - 文字统一浅色（白/灰白/红/黄/金），深色背景不用深色字。
    - **已搁置/待做（工作记录）**：天赋实际效果多为占位（涅槃、巧遇、好奇心、暴击/嗜血/起势/格挡/坚守、风息/比翼等）只做文本未接逻辑；敌人技能暂无真实伤害效果（仅介绍）；结界/境界/全场效果仅搭展示位（field）未接技能；束缚/冰冻状态已建模型但暂无技能施加。
  - **未实现(见文档 01·总览)**：完整剧情线、图鉴、商店定价、更多敌人、完整伤害乘区、心理压力、剩余队友（许泠朦/叶唯安/潘天宇）数据

## 五、游戏设计要点（基于权威规格书，供 Agent 开发时参考）

- **类型**：回合制战斗 + 文字剧情 + 少量推理、数值养成/阵容搭配、少量生存经营的单机游戏；非肉鸽。
- **两个核心模式（共用一张格子地图）**：探索（收集资源）与战斗（回合制）；战斗结束强制传送回进入格。
- **天数系统**：每天随机重生地图（尺寸随天数渐进）；睡觉=进下一天+自动存档；health 归零即游戏结束。
- **战斗**：移动即视为回合结束；移动前主动放技能(Q/双击)、移动后自动释放已选技能（队伍一二三号位，已用跳过）；朝向决定部分攻击；方向性技能需方向上有敌人才生效。
- **状态系统**：角色/敌人各自状态栏；buff 黄框、debuff 红框、中性灰；主角额外记录全场结界/境界；状态**点击**查看，词条【】**悬浮**查看；状态过多横向滚动。
- **伤害公式/元素反应**：见 docs/04·战斗系统（pseudocode + 附录B 反应表）。
- **队伍**：含主角最多3人；攻+辅每角色最多带3个；每回合每角色至多1技；编队可调换123号位、角色页可自选携带技能。
- **未实现/待定**：潘天宇数据、敌人数据、商店数值、数值平衡、剧情分支、图鉴内容、行动力/心理压力系统等 —— 均在 docs/01·总览 的「十五、待定清单」，**未确认前不要脑补**。

## 六、美术 / 文字定位（规格书第十章）

- 极简：文字+数值为主体，极少立绘（有也用图标），区域用背景/区域色。
- 元素着色：火red/水blue/草green/雷purple/冰cyan/风lightBlue/岩brown。
- 词条：技能描述【】内效果名=词条，下划线+悬浮弹窗解释。
- **前端口径一律中文**。

## 七、Agent 操作规范（用户明确要求，必须遵守）

1. **所有锚点文件内容，只能来自本次对话**。禁止从其他会话的记忆、用户画像标签、经验回顾里搬运任何未经用户再次确认的信息。
2. **每次修改代码或修改网页后，必须同步更新本锚点文件**。更新时整理分类、删除过时条目，空间有限别堆成垃圾场。
3. **修改代码时，直接删除旧内容，不要保留删除线+新内容双写结构**。

## 八、给后续 Agent 的快速上手流程

```javascript
// 1) 先读本文件
// 2) 再读仓库当前状态
get_file_contents({ owner: "zengxiaohao123", repo: "html-game", path: "" })
//  玩法机制权威来源：docs/玩法机制-01~06
//  代码结构：index.html 引入 css/style.css + js/{data,save,map,explore,combat,ui,main}.js
// 3) 改代码：用 create_or_update_file / push_files 推 main 分支（push_files 必须内联 files[].content）
// 4) Cloudflare 自动部署后，用 WebFetch 验证
// 5) 改完代码不要忘了：回到本文件按第七条规则同步更新
```

## 九、部署故障排查

如果 Cloudflare 网址打不开但 GitHub 有文件：
1. Cloudflare Pages 控制台 → html-game → Deployments，看最新构建是否成功
2. 如失败，查看构建日志错误（常见：路径写错、main 分支未选中、index.html 不在根目录）
3. 修复后重新 commit 触发自动重部署
4. **页面只显示标题、无主菜单按钮，且已确认线上 JS 就是新的（含 `const $=id=>document.querySelector(id)`）→ 多为浏览器/边缘缓存命中旧 JS，提示用户 硬刷新(Ctrl+F5) 或 无痕窗口，或带唯一 query 参数访问。若线上 JS 仍是旧的 `getElementById` 版本 → 是旧部署，重新 commit 触发 CF 重部署。**