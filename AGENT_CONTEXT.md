# AGENT\_CONTEXT.md

> 给后续 AI Agent 看的锚点文件。新对话直接读本文件即可完整继承上下文。
> ⚠️ 本文件的每一条内容**只能来自本次对话（用户亲口说过的 + Agent 亲身验证过的）**，禁止引用其他会话的记忆或用户画像标签。

***

## 一、项目元信息（已确认）

| Field           | Value                                        |
| --------------- | -------------------------------------------- |
| GitHub 仓库       | `zengxiaohao123/html-game`                   |
| GitHub 主页       | `https://github.com/zengxiaohao123`          |
| Cloudflare 部署网址 | `https://html-game-ap9.pages.dev/`           |
| 默认分支            | `main`                                       |
| 构建方式            | 纯静态 HTML/JS，无构建步骤，Build command 留空，输出目录填 `/` |
| Cloudflare 绑定   | ✅ 已绑定本仓库，main 分支有新 commit 时自动部署（约 10 秒生效）    |
| 部署网址国内直连        | ✅ 移动/联通/电信均无需翻墙                              |

## 二、已明确的约束条件（用户说过的原话）

- 单机游戏，**没有后端服务器**。存档用浏览器 localStorage 或 IndexedDB，纯本地保存。

- 不考虑排行榜，不读取其他玩家数据，不向任何外部服务发送用户游戏数据。

- 代码全部存 GitHub 仓库，Cloudflare Pages 自动托管。

- 不引用需要翻墙的外部 CDN（国内能访问的 jsDelivr / unpkg 可以）。

- **前端一切展示文本必须中文**（中文游戏）；代码/变量可英文，前端渲染必中文。

- 逃跑系统尚未做，目前只显示逃跑速度，暂不做真正逃跑；敌人的普通攻击也算一种技能，写入技能信息。

- 文字配色：**一律浅色**（白/灰白/红/黄/金），慎用灰黑；仅在白色背景上才可用黑字。

- **技能等级制度（09-04）**：文档带「等级1」的条目有等级概念，会升级的数值用**黄字**展示；无等级的固定效果不随等级。测试期等级一律=1，暂不做升级。

- **羁绊等级（09-04）**：除主角外每队友独立羁绊等级，技能/天赋等级=该角色羁绊等级；升级需10好感，测试期无好感来源，仅做框架、暂不升级。

- **暴击（09-04）**：所有角色初始暴击率0，暴击伤害固定+100%（×2）；暴击率最终限幅0~100%；暂不做改暴击伤害的手段。「下一次攻击暴击率+100%」生效期间单位+100%暴击率，用了一次攻击型技能后提前失效。

- **地图边框（09-04）**：随机生成地图时，有效地图(内区)外围再包一圈「地图外」(void) 边框，不影响实际有效地图生成。

- **主界面（09-04）**：主界面扩大为整个屏幕，选项适当放大；未进入游戏前完全遮挡后方内容（改为不透明）。

- **合成系统（09-04）**：只有配方制合成（禁止自由合成）；非战斗时可打开（战斗禁开）；每个配方有独立「合成」按钮；可调一次合成数量（滑块/加减）；点配方里的资源/物品可预览说明；产物直接入背包。快捷键 E。

- **载具系统（09-04）**：无论探索/战斗都可打开「载具」界面；选择/切换当前移动方式；每次真实移动消耗 1 个已选载具，耗尽自动切回徒步；同种载具可有多个、独立存于不同槽位（格子式逐格展示，非逐行）；检查点每件载具的使用次数（无限次不标注）。快捷键 T。仅作便利移动，遵循探索每移耗1 AP、战斗每回合移1次的约束。

- **ESC 退回一层（09-04）**：几乎所有界面弹窗按 ESC 退回一层；主要游玩页无弹窗时 ESC 打开设置；主界面/游戏结束界面 ESC 无效果；不影响行动记录区、地图本身、角色技能区、选项信息区。

- **商店（09-04）**：与合成同款左右两列大面板；仅非战斗可开（战斗禁开）；测试期商店按钮无视区域恒显示；金币可用购/售，出售价=购买价×50%；部分物品仅可购不可售；购买/出售共用 1 个滑块设数量；点物品可预览说明并显示持有数量；测试初始金币=20。购买/卖出的不足与超额提示**直接显示在商店界面内**（shopMsg），不进行动记录。

- **背包（09-04）**：全屏格子化（同种自动合并显示数量、载具不在此）；点物品名可预览说明；部分物品（果子/绷带/风干肉/草药膏）有「使用」按钮，每次用1个，用尽该格消失、后续顶位；金币独立显示在顶部；战斗中不可使用物品。

- **战斗存读档（09-04）**：战斗中<b>编队/睡觉/商店/合成均禁止</b>（图标行统一置灰 .dis，详见下方「战斗中统一禁开」）；但<b>允许存档/读档/返回主界面（不存档）</b>。战斗中存档一律以「本次战斗开始那一瞬间」为存档点：生命、装载载具都回到战斗开始，敌我状态全新（无状态/未用技能）。读回战斗存档 → <b>直接处于战斗中</b>，剧情区立即被角色技能区替换（mode=combat），敌我状态均按战斗开始。实现：`combatState.startSnapshot`（initCombatState 记录 heroHp/vehicles/vehicleSel/enemy）、saveGame 战斗分支回退并写 `snap.combat`、`reenterCombat()` 读档重回；返回主界面已移除 showMenu 的自动存槽0（不存档）。

- **文本** **`%%`** **修正（09-04）**：描述模板已自带 `%` 的 pct 占位（主角·暴击、陆悠悠·风息）删除模板侧的多余 `%`，仅保留 `s.pct` 追加的单 `%`，不再出现 `%%`。

- **战斗中统一禁开（09-04）**：战斗中编队/睡觉/商店/合成四项一律置灰 `.dis` 且守卫统一提示「战斗中无法使用该功能。」（`renderIconbar` blocked 集合含四项；openFormation/sleep/openShop/openCraft/doCraft 统一文案）。背包仍可打开**查看**。

- **背包战斗态（09-04）**：战斗中可打开背包查看，但隐藏「使用」按钮（`renderInventory` 的 useBtn 只在非战斗且 itemUsable 时显示）；被动/持续生效的物品不受影响。useInvItem 仍有战斗守卫兜底。

- **WASD/键盘修复（09-04）**：探索与战斗的键位处理原在 combat.js 单个 document bubble `keydown`，两态共用却因焦点/宿主吞键失效。已改为 main.js 的 `handleKeys`（**capture 阶段**监听，探索 WASD 在弹窗打开时不误移、每次一步 `ev.repeat` 守卫；战斗 WASD/Q/1·2·3选技能/F1·F2·F3切人）；`ensureKeyFocus()` 在进入世界与进入战斗时强制 `window.focus()`+`body.focus()`，确保页面持有键盘焦点。

## 三、连接状态（Agent 2026-09-01 实际测过）

- GitHub MCP **写入权限已实测**；GitHub → Cloudflare Pages webhook **已绑定**（改 main → 自动部署）。

## 四、当前进度

- [x] GitHub 仓库创建，Cloudflare Pages 绑定；MCP 读写链路实测通过；本锚点写入仓库

- [x] 玩法机制文档**已拆分为多篇（docs/目录 01~06）**，为唯一权威来源（SSOT）。

- [x] **游戏本体已实现并部署（可试玩）**
  - 核心循环：每天随机地图、WASD/点格+前往移动+朝向、资源递进采集、宝箱/事件、回合战斗(WASD/Q/点敌选目标)、主角5攻击技、敌人逼近/贴身攻击、基础元素反应、睡觉进下一天、8槽localStorage存档、健康归零GameOver、主菜单/背包/角色/编队/任务/商店

  - 已修复：战斗击杀越界访问；`$` 误用 getElementById；goBtn 永久禁用；探索 WASD 连续移动

  - **模块化(09-03)**：`index.html` + `css/style.css` + `js/{data,save,map,explore,combat,ui,main}.js`，经典全局 script 按依赖顺序加载（data→save→map→explore→combat→ui→main）。

  - **大改版(09-03)**：队友占位(夏阳·火/陆悠悠·风)、移动逻辑(探索转向返还AP、战斗朝障碍计已移动)、攻击朝向、UI区域分工(左行动记录/中地图/右信息/底部剧情或战斗技能)、地图不规则+随机出生+缩放拖拽、敌人红色朝向三角+范围指示器、全屏面板、行动记录/剧情区清空。

  - **状态系统+UI(09-03)**：真实状态系统(`ally[k].statuses`/`enemy.statuses`/`field`)；状态栏(黄/红/灰、横滚、主角额外全场)；敌人技能数据+信息区两页(属性/详细技能)；技能/天赋数值化；编队调换+角色页携带技能；敌人HP上调(420/330/520)；行动记录约1/3屏宽；选中技能浅金背景、辅助蓝框。

  - **状态/词条区分+细节(09-03)**：状态=**点击**查看弹窗、词条=**悬浮**查看（TERM\_KEYS 中文映射）；删剧情区/角色技能区标题；地形约80%可通行+命名；技能指示器不含不可通行格、敌方指示器仅查看该敌才显示；信息区刷新/敌人意图详细/元素附着并入状态栏/已探索格显示空地；属性回独立数值行、状态栏加大；文字统一浅色。

  - **修正(09-03)**：

    - 攻防血/逃跑速度写回独立数值行（角色卡不显示数值）；元素属性随角色名展示，**不放进数值行**。

    - 天赋点击在**天赋附近**弹窗（委托事件+openPopoverNear，超出屏幕底部自动**翻转**到上方防遮挡）。

    - 技能描述**写出公式**（如 攻击力×100%）并在括号标当前数值；词条【】**悬浮**查看已修复。

    - 战斗移动：**点击格子需在信息区点「前往」确认**才移动；WASD 直接走；两者等价，目标可为空地/障碍/实体/地图外——非空地则只转向并计本回合已移动。

    - 敌人指示器：仅查看该敌人所在格时显示，点其他格不显示。

    - 敌人信息区"属性/详细技能"两字改白；敌人移动方式写入技能页。

    - 未注明持续时间的 buff/debuff 默认持续整场，不标时长。

    - 行动记录**指明伤害元素类型**并着色（物理/火元素伤害红字等，真实伤害需说明）。

    - 角色页切换角色名字按钮字体改白；切换时**即时高亮**被选按钮（renderCharacters 整体重渲染）。

    - **战斗失败后该格敌人保留**（endCombatByDefeat 防御性恢复敌人内容，可再次作战）。

    - **已搁置/待做（工作记录）**：天赋实际效果多为占位（涅槃、巧遇、好奇心、嗜血、起势、格挡、坚守、风息攻击加成等）只做文本未接逻辑；敌人技能暂无真实伤害效果（仅介绍）；结界/境界/全场效果仅搭展示位（field）未接技能；束缚/冰冻状态已建模型但暂无技能施加；吸血/易伤/减伤等乘区未接入。

  - **技能等级+羁绊+暴击+地图边框+主界面(09-04)**：

    - **等级体系**：`js/data.js` 给带等级的条目加 `scal`（{数值:{base每级数值,grow每级增量,pct}}）+ 描述占位符 `{key}`；`lvDescText()`/`tierValue()`/`entryLevel()` 渲染。主角等级存 `G.proLevels`（按条目 id），队友等级=羁绊等级（`G.bonds[key].level`）。会升级的数值一律 `class="lvlup"` 黄字；带等级条目展示名带「·等级N」（`skillDisplayName`/`talentDisplayName`）。涉及：主角天赋(暴击/嗜血/起势/格挡/坚守)、夏阳天赋(无所畏惧)+技能(鼓舞)、陆悠悠天赋(巧手/蹁跹/风息)。

    - **羁绊**：`newGame()` 初始化 `G.bonds`（每队友 level1/affinity0）；编队页与角色页显示羁绊等级；技能区不显示羁绊。

    - **暴击**：`baseCritRate()`（主角·暴击给人+3%、陆悠悠·风息给自身+30%，随等级）+ 临时 `charCritRate()`（含屏息/比翼+100%，限幅0~100%）；`resolveSkill` 里暴击→×2并记录「暴击！」；攻击技后 `consumeCritBuff()` 提前消耗；陆悠悠暴击触发 `triggerBiyi()`（比翼：其余我方下次攻击暴击率+100%）。`charAttrsHTML()` 数值行新增「暴击率」实时值。

    - **地图边框**：`generateMap()` 有效尺寸=内区 n，总边长 n+2，外围一圈 `void`（不可通行），内区生成/出生/连通不变。

    - **主界面**：`#menuOverlay` 背景改不透明 `#14161c`，标题放大、选项按钮放大居中，完全遮挡后方。

    - **合成**：新增 `js/craft.js`（快捷键 E，战斗禁开）。`CRAFT_RECIPES` 配方制；`craftQty` 记录每配方待合成数量；滑块/加减调量；点击配方里的资源/物品 `craftPreview` 弹出预览（`openPopoverNear`）；`doCraft` 校验-扣除-产出-入背包；其实物产物 key 见 `js/data.js` 的 `ITEMS`（暂占位无使用逻辑）。

    - **载具**：新增 `js/vehicle.js`（快捷键 T，探索/战斗均可开）。`VEHICLES` 库 + `G.vehicles`（槽位数组）+`G.vehicleSel`；`useVehicleOnMove()` 在探索 `moveExplore` 与战斗的 `confirmCombatMove`/`combatMove` 成功移动后调用，消耗 1 次并数尽自动切回徒步（id0）；格子式 `.vslot` 展示，选中浅黄 `.sel`。

    - **ESC**：`ui.js` 新增 `modalStack` 弹窗层级栈；`openModal(title,html,size,{replace:true})` 支持原地刷新不压栈（合成/载具数值变化用途）；`modalBack()` 退回一层；ESC 三大分支见上「约束」。

    - **背包物品名**：`openInventory` 改用 `itemName(k)` 统一显示资源与制作物中文名。

    - **图形定位修正**：`renderIconbar` 的商店隐藏改用 `findIndex(s=>s[0]==='商店')`，不再依赖固定下标 6（因新增了合成/载具按钮）。

    - **合成改两列+物品预览**：合成页面 `.cwrapper` 改为左右两列；配方卡片增高文字、滑动条缩短；点击配方中任何资源/物品（含产物）用 `data-key` + ui.js 委托 `.craftlink` 弹窗预览 `itemName/itemDesc`。

    - **载具行动记录**：`selVehicle` 切换时记录「当前装载了xx」；`useVehicleOnMove` 有限次数载具在移动后记录「使用了xx移动，剩余n次」（无限次不提示）。

    - **商店**：`openShop`/`renderShop`+`SHOP_ITEMS`（data.js）实现左右两列购/售（`shopTrade`），`shopQty` 记录每物品交易数量（购/售共用），出售价=`Math.floor(buy*0.5)`，`campkit` 设为仅购不可售；`newGame` 初始金币改 20。

    - **战斗存读档（09-04）**：本地补回缺失的 `js/save.js`（分支战斗存档，读回存档时 `G.combat` 携带战斗存档标记；`MAX_SAVES` 沿用 data.js 定义）。combat.js 抽 `initCombatState`/`reenterCombat`，`startSnapshot` 记录战斗开始瞬间（heroHp/vehicles/vehicleSel/enemyKey/enemyHp/enemyPos）；saveGame 战斗分支把生命与载具回退到战斗开始并写 `snap.combat`；loadIntoWorld 读到战斗存档即 `reenterCombat` 直接进入战斗（剧情区变角色技能区）。战斗中禁编队/睡觉/商店/合成（openFormation/sleep/openShop/openCraft/doCraft 守卫统一文案 + 图标行 `.dis` 置灰四项），设置面板战斗态文案「存档（回本次战斗开始时）」；`backToMenu` 清空战斗且不再自动存槽0。

    - **WASD/键盘修复（09-04）**：combat.js 原单一 document bubble keydown 两态共用，因焦点/宿主吞键导致探索+战斗 WASD 全失效。改为 main.js `handleKeys`（capture 阶段监听；探索 WASD 弹窗开不误移 + `ev.repeat` 一格；战斗 WASD/Q/1·2·3/F1·F2·F3）。新增 `ensureKeyFocus()`（window.focus+body.focus）在 `loadIntoWorld` 正常分支与 `enterCombatMode` 调用。

    - **背包战斗态（09-04）**：`renderInventory` 战斗中隐藏「使用」按钮（`(!combatState && itemUsable(k))`），战斗仍可打开背包查看；useInvItem 战斗守卫兜底。

    - **战斗结束恢复UI（09-04·本次）**：修复战斗中禁用的 编队/睡觉/商店/合成 在战斗结束时图标未恢复原样（此前须等次日 `renderIconbar` 才恢复）。根因：`endCombat`/`endCombatByDefeat` 置 `combatState=null` 后未重渲染图标行。两函数末尾补 `renderIconbar()`。

    - **物品正式化（09-04·本次）**：按用户原稿把占位物品（绷带/风干肉/草药膏/营火套件/疾行油）全部替换为正式物品，删除旧数据：
      - 资源新增 `iron` 铁块：相对罕见的基础材料，**非自然资源/城市资源**，无法常规探索获得，仅特殊事件/特殊敌人/商店购买；商店购价6、可出售。`NATURAL_RESOURCES`（陷阱随机池）= wood/flax/fruit/rawMeat（不含铁块、空瓶子）。
      - 可直接使用食物 `FOOD`：果子(回20血+20%概率+1健康)、生肉(回20血)、熟肉(回70血)。`useInvItem` 改为 `foodHeal(k)`（含篝火加成）。
      - 合成产物 `CRAFT_RECIPES` 重写：熟肉(木1+肉1)；永久物品含 `scale` 递增耗材（木棒 6木+2/个、布衣 8亚麻+2、帐篷 4木6亚麻+1/个、陷阱 4木+2、被子 6亚麻+2、铁剑 10铁+2、盔甲 14铁+3）；篝火 4木 全局至多1。递增按「第 i 件耗 base+scale*(已合成+i)」累计，一次合成多件也受逐件递增（`recipeNeedTotal`/`craftMax`）。
      - 永久物品 `grantPermanentItem`：获得即入背包并永久生效（不可用/不可消耗/不可出售）：木棒→天赋暴击+1、布衣→格挡+1、匕首→嗜血+1、皮衣→坚守+1、铁剑→起势+1、盔甲→格挡+1且坚守+1（均 `bumpPro` 加 `G.proLevels`）、帐篷→行动力上限+1(补1AP)。篝火/陷阱/被子无即时数值，分别在食物回血/睡觉结算。
      - 商店 `SHOP_ITEMS`：资源(木2/亚麻3/果子2/生肉4/空瓶5/铁6，均可售；出售=购价×50%) + 匕首(基32)与皮衣(基20) 仅购不可售，`priceGrow=4`：每获得1个该物商店售价+4（`itemBuyPrice` 动态价，购买走 `grantPermanentItem`）。篝火/陷阱/木棒/布衣/铁剑/盔甲/被子 不在商店。
      - 睡觉接入永久物品：`sleep()` 中 被子 每床回30血并叠加为**1次总和** 记录在行动记录；陷阱 每个独立50%随机1自然资源，`NATURAL_RESOURCES` 池、最后**综合汇报**。
      - 新开局背包初始化补 `iron:0`。旧存档里的占位物品 key 不再能解析为名称，属历史存档兼容忽略项。

- [ ] **未实现(见文档 01·总览)**：完整剧情线、图鉴、商店定价、更多敌人、完整伤害乘区、心理压力、剩余队友（许泠朦/叶唯安/潘天宇）数据

## 五、游戏设计要点（基于权威规格书，供 Agent 开发时参考）

- **类型**：回合制战斗 + 文字剧情 + 少量推理、数值养成/阵容搭配、少量生存经营的单机游戏；非肉鸽。

- **两个核心模式（共用一张格子地图）**：探索（收集资源）与战斗（回合制）；战斗结束强制传送回进入格。

- **天数系统**：每天随机重生地图；睡觉=进下一天+自动存档；health 归零即游戏结束。

- **战斗**：移动即视为回合结束；移动前主动放技能(Q/双击)、移动后自动释放已选技能；朝向决定部分攻击；方向性技能需方向上有敌人才生效。

- **状态系统**：角色/敌人各自状态栏；buff 黄框、debuff 红框、中性灰；主角额外记录全场结界/境界；状态**点击**查看、词条【】**悬浮**查看；状态过多横向滚动。

- **伤害公式/元素反应**：见 docs/04·战斗系统（pseudocode + 附录B 反应表）。

- **队伍**：含主角最多3人；攻+辅每角色最多带3个；每回合每角色至多1技；编队可调换123号位、角色页可自选携带技能。

- **技能等级与羁绊(09-04)**：主角技能等级=自身各条目的 `G.proLevels`；队友技能/天赋等级=该角色羁绊等级；测试期全=1。羁绊升级需10好感（暂无来源）。

- **暴击(09-04)**：初始暴击率0，暴击+100%（×2）；上限/下限=100%/0%；暂不可改暴击伤害。

- **未实现/待定**：潘天宇数据、敌人数据、商店数值、数值平衡、剧情分支、图鉴内容、行动力/心理压力系统等 —— 均在 docs/01·总览「十五、待定清单」，**未确认前不要脑补**。

## 六、美术 / 文字定位（规格书第十章）

- 极简：文字+数值为主体，极少立绘（有也用图标），区域用背景/区域色。

- 元素着色：火red/水blue/草green/雷purple/冰cyan/风lightBlue/岩brown。

- 词条：技能描述【】内效果名=词条，下划线+悬浮弹窗解释。

- **会随等级提升的数值用黄字（.lvlup #ffd23f）展示**。

- **前端口径一律中文**。

## 七、Agent 操作规范（用户明确要求，必须遵守）

1. **所有锚点文件内容，只能来自本次对话**。禁止从其他会话的记忆、用户画像标签、经验回顾里搬运任何未经用户再次确认的信息。
2. **每次修改代码或修改网页后，必须同步更新本锚点文件**。更新时整理分类、删除过时条目。
3. **修改代码时，直接删除旧内容，不要保留删除线+新内容双写结构**。

## 八、给后续 Agent 的快速上手流程

```javascript
// 1) 先读本文件
// 2) 再读仓库当前状态
get_file_contents({ owner: "zengxiaohao123", repo: "html-game", path: "" })
//  玩法机制权威来源：docs/玩法机制-01~06
//  代码结构：index.html 引入 css/style.css + js/{data,save,map,explore,combat,ui,main}.js
// 3) 改代码：用 create_or_update_file / push_files 推 main 分支（push_files 必须内联 files[].content）
// 4) Cloudflare 自动部署后，用 WebFetch 验证
// 5) 改完代码不要忘了：回到本文件按第七条规则同步更新
```

## 九、部署故障排查

如果 Cloudflare 网址打不开但 GitHub 有文件：

1. Cloudflare Pages 控制台 → html-game → Deployments，看最新构建是否成功
2. 如失败，查看构建日志错误（常见：路径写错、main 分支未选中、index.html 不在根目录）
3. 修复后重新 commit 触发自动重部署
4. **页面只显示标题、无主菜单按钮，且已确认线上 JS 就是新的（含** **`const $=id=>document.querySelector(id)`）→ 多为浏览器/边缘缓存命中旧 JS，提示用户 硬刷新(Ctrl+F5) 或 无痕窗口，或带唯一 query 参数访问。若线上 JS 仍是旧的** **`getElementById`** **版本 → 是旧部署，重新 commit 触发 CF 重部署。**
